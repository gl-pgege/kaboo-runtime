#!/usr/bin/env node
/**
 * Generates docs/api-inventory.md — a flat, committed index of every public
 * symbol, derived from the TypeDoc markdown output in docs/api. Run
 * automatically by `yarn docs:api` (after `typedoc`).
 *
 * Zero dependencies; deterministic (sorted) so CI can diff it for drift.
 */
import { readdirSync, existsSync, writeFileSync } from "node:fs";
import { join } from "node:path";

const ROOT = new URL("..", import.meta.url).pathname;
const API_DIR = join(ROOT, "docs", "api");
const OUT = join(ROOT, "docs", "api-inventory.md");

const KINDS = [
  { dir: "functions", label: "Functions" },
  { dir: "classes", label: "Classes" },
  { dir: "interfaces", label: "Interfaces" },
  { dir: "type-aliases", label: "Type aliases" },
  { dir: "variables", label: "Variables" },
];

function listSymbols(kindDir) {
  const dir = join(API_DIR, kindDir);
  if (!existsSync(dir)) return [];
  return readdirSync(dir)
    .filter((f) => f.endsWith(".md"))
    .map((f) => f.replace(/\.md$/, ""))
    .sort((a, b) => a.localeCompare(b));
}

let out = "# API inventory\n\n";
out +=
  "Generated by `yarn docs:api` from the TypeDoc output. Every public export of " +
  "`kaboo-runtime` is listed here. Do not edit by hand.\n";

for (const kind of KINDS) {
  const symbols = listSymbols(kind.dir);
  if (symbols.length === 0) continue;
  out += `\n## ${kind.label}\n\n`;
  for (const name of symbols) {
    out += `- [\`${name}\`](api/${kind.dir}/${name}.md)\n`;
  }
}

writeFileSync(OUT, out);
console.log(`Wrote ${OUT}`);
